import {SquareRoot} from "../../src/models/square-root";

describe("SquareRoot.apply", () => {
    test("fhjfhfj", () => {
        const testNumber: number[] = [0, 1]
        expect(new SquareRoot().apply(testNumber)).toEqual([1, 0]);
    });


});